import { ProductPopularityPipe } from './product-popularity.pipe';

describe('ProductPopularityPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductPopularityPipe();
    expect(pipe).toBeTruthy();
  });
});
